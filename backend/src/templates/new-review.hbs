<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Review</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #F59E0B;
            color: white;
            padding: 30px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }
        .content {
            background-color: #f9fafb;
            padding: 30px;
            border-radius: 0 0 8px 8px;
        }
        .button {
            display: inline-block;
            padding: 15px 40px;
            background-color: #F59E0B;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            margin: 20px 0;
            font-weight: bold;
        }
        .review-box {
            background-color: #FFFBEB;
            border-left: 4px solid #F59E0B;
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .stars {
            color: #F59E0B;
            font-size: 20px;
        }
        .footer {
            text-align: center;
            color: #6b7280;
            font-size: 12px;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>⭐ New Review Received</h1>
    </div>

    <div class="content">
        <p>Hi {{userName}},</p>

        <p>You received a new review from <strong>{{reviewerName}}</strong>{{#if productName}} on your product <strong>{{productName}}</strong>{{/if}}.</p>

        <div class="review-box">
            {{#if rating}}
            <div class="stars">
                {{#each stars}}⭐{{/each}}
            </div>
            {{/if}}
            <p><strong>{{reviewerName}}</strong> said:</p>
            <p style="font-style: italic;">"{{reviewPreview}}"</p>
        </div>

        <center>
            <a href="{{actionUrl}}" class="button">View Full Review</a>
        </center>

        <p>Customer feedback helps you improve your products and build trust with other buyers.</p>

        <div class="footer">
            <p>You're receiving this email because you've enabled review notifications.</p>
            <p><a href="{{actionUrl}}" style="color: #F59E0B;">Manage your notification preferences</a></p>
            <p>&copy; {{year}} TCG Marketplace. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
